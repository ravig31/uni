`/etc/hosts serves` local, static lookup table for ips and their hostname 
- local dns
- blocking websites

`/etc/resolv.conf` tells the system which default DNS servers to use when lookup is not found /etc/hosts
